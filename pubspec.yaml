name: ordem_paranormal_rpg
description: Kit de Gestão de RPG Ordem Paranormal com Flutter e Firebase
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Banco de Dados Local
  sqflite: ^2.3.0
  path_provider: ^2.1.1
  path: ^1.8.3

  # UI & Navigation
  cupertino_icons: ^1.0.2

  # Utilities
  share_plus: ^12.0.0
  uuid: ^4.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
